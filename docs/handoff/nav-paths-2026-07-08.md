# Navigation paths & broken references (G40-318)

_Done 2026-07-08. Part of Epic **G40-312** (scale/production-readiness)._

## Audit result

Scanned every `href`/`src` across all pages **and** the shared header/footer JS (where nav/footer links now live), checking against the real on-disk filenames (case-exact).

| Check | Result |
|---|---|
| Root-absolute internal paths (leading `/` → 404 on subdir) | **0** — all internal refs already relative ✅ |
| Case mismatches (Linux case-sensitive) | **0** ✅ |
| Home / logo links → `index.html` | ✅ header `LINKS.homepage='index.html'`, footer `gf-logo-wrap` → `index.html` |
| Broken internal links | **1 real** (fixed) + 8 known-missing hero clips |

## Fixes

- **Legacy broken link:** `gopher-request-101.html` linked `terms-of-service.html`; the real file is `gopher-terms-of-service.html`. Corrected.
- **External `gophergo.io/wp-content` images localized (13 total).** These were hot-linked from the live WordPress site and would 404 the moment gophergo.io is replaced. Downloaded, converted to WebP (q82, ≤1400px), saved to `assets/img/` (`wp-*.webp`, `blog-*.webp`), and repointed across 7 pages (blog, index, our-story, request, request-101, go, prohibited-list). **0 wp-content image hotlinks remain.** `mailto:*@gophergo.io` addresses (27) were correctly left untouched.

## Not a bug (documented)

- **8 hero clips** (`hero-media/clip-1..4.{webm,mp4}`, referenced by `gopher-connect.html`) remain missing — genuine video-asset production, not a link error. The hero degrades gracefully to its poster. See `docs/handoff/missing-files.md`.

## Verified in browser

Blog + index render with all localized images loading (no 404s); audit re-run confirms only the 8 hero clips remain unresolved.

## Deploy note

The 13 new `assets/img/wp-*` / `blog-*` files must be deployed. Deploy the whole `Final/` tree.
