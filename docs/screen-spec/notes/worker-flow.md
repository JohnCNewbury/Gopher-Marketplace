VERDICT: REFERENCE STORYBOARD — a flow overview, not a single buildable screen.

BEHAVIOUR
- End-to-end worker journey frames (see → accept → work → complete → paid) for
  orientation. Build the individual screens; use this only to check sequence.
- Counter/accept rules, completion evidence, and payout timing are all owned by
  their canonical sections (gopher-go-canonical.html) — not by this storyboard.

ENDPOINTS / BACKEND SEAMS
- None directly; each frame's real screen carries its own seams.
