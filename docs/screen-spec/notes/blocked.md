VERDICT: NET-NEW — no live worker-side block list.

BEHAVIOUR
- "Block Requesters" (spelling canon: Requester, -er). Top-level sidebar item
  directly under Previous requests (owner 7/23).
- Blocking removes that requester's future requests from this worker's feed.

ENDPOINTS / BACKEND SEAMS
- Block-list table + broadcast filter exclusion; server-enforced.
