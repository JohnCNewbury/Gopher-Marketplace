VERDICT: ADAPT — simple form; route to the existing support stack.

BEHAVIOUR
- Contact form (topic + message) + support contact line. Support email flows into
  Intercom (support@ Gmail→Intercom forwarding is live and verified).

ENDPOINTS / BACKEND SEAMS
- POST to support ticket creation (Intercom) — do not build a parallel inbox.
