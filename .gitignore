# Ephemeral session safety-net (pre-chrome-dedup HTML originals).
# Remove once the repo is confirmed good.
_backups/

# Local, machine-specific Claude Code settings — not for version control.
.claude/settings.local.json

# Redundant archive zips — duplicate tracked folders.
/Final.zip
/Pic:Video Assets.zip

# Rebuild spares / high-res asset masters — NOT live-site deps; backed up
# separately (per CLAUDE.md). Excluded to keep the repo lean. Un-ignore to track.
/source-images/
/source-assets/
/Pic:Video Assets/
/Age-Restricted/
/Convenience Store/
/Home Screen/
/Local Favorites/
/Restaurants & Food Trucks/
/Service Providers/

# macOS cruft
.DS_Store
# Prototype working-area assets — keep out of the published tree, keep the prototype file
_prototypes/Go/*
!_prototypes/Go/gopher-go-prototype.html
_prototypes/Request/*
!_prototypes/Request/gopher-request-prototype.html
