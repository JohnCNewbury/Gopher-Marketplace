VERDICT: ADAPT — home is new IA over live data feeds.

BEHAVIOUR
- Worker home in zones: Local jobs (live counts by category), Work settings summary,
  Deals (gated by __dealsLive — outside the launch DMA the whole section is removed),
  Perks (Gopher Rewards · Refer App · + Service Provider Deal tile), Tools
  (gopher iQ pill — real, worker-scoped), Account.
- The iQ pill opens the worker-scoped sheet: Find work near me / Payout Account /
  Work Settings / Help Center; answers via the shared 3-tier brain.
- SP Deal tile states: eligible → offer form; ineligible → dim + lock + criteria popup.

ENDPOINTS / BACKEND SEAMS
- Jobs counts from the broadcast feed; deals gate + SP eligibility from backend;
  work-settings summary reads the saved profile.
