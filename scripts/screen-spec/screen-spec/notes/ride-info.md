VERDICT: ADAPT — vehicle data exists live; the evidence tiles + tri-state are new.

BEHAVIOUR
- Vehicle details + evidence tiles: front photo (head-on), rear photo (plate visible),
  registration, insurance (docs accept PDF). Tile states: on-file / updated / need.
- Changing any vehicle detail flips all four tiles to "Update needed" (per-vehicle
  evidence). Feeds the work-settings ride gate.

ENDPOINTS / BACKEND SEAMS
- Vehicle record + document storage on worker profile; server-side gate re-check.
