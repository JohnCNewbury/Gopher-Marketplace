VERDICT: ADAPT — offer entry; suggested-offer model + bids rules are canon.

BEHAVIOUR
- Worker offer entry; MUST be > $0 (owner canon 7/17 — no $0 offers anywhere).
- Suggested-offer guidance uses the NC pricing column platform-wide (worker-pay
  forced to NC values). The below-average-offer nudge exists live on iOS ONLY —
  rebuild makes it cross-platform.
- Bids mode: available for service categories; Delivery/Ride/Other are offer-only
  (bids hidden — same rule Connect applies). "I'll select my worker" naming canon.

ENDPOINTS / BACKEND SEAMS
- Suggested-offer model read; per-worker pay computed as offer × crew where crew
  applies (display only — one hire, see step 3).
