VERDICT: ADAPT — submitted state; broadcast mechanics carry, promises must reconcile.

BEHAVIOUR
- "Request submitted" confirmation: matching status, edit/cancel affordances until
  accepted, then hard-locked (live: requester hard-locked after accept; no automatic
  $5 cancellation fee exists live — manual admin flag only; canonical cancellation
  policy adds the conditional $5 trees, see cancellation canon).
- Favorites head start: live gives MY Gophers a 1-second broadcast head start +
  approval bypass while app copy promises "5 minutes" — rebuild picks one truth.

ENDPOINTS / BACKEND SEAMS
- Broadcast fan-out, favorite prioritization, expiry timer; push notifications on
  accept (build tap-through — live handlers are empty).
