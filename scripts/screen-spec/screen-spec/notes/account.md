VERDICT: ADAPT — account hub matching the canonical sidebar IA (owner 7/23).

BEHAVIOUR
- Canonical structure: top-level Dashboard-equivalents (Previous requests, Inbox);
  Account = Business info, Personal info, Work settings & ratings, Payment info;
  Rewards divider = Refer Gopher; Resources = Ask Gopher iQ, Visit site,
  Send feedback; Block Requesters top-level under Previous requests.
- "← Back to main page" leaves the dashboard but keeps the session.

ENDPOINTS / BACKEND SEAMS
- Profile/settings reads; sign-out clears session only.
