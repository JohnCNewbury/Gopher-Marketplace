VERDICT: ADAPT — same messaging layer as the worker inbox.

BEHAVIOUR
- Threads with the hired worker(s); per-message delete via the shared inbox module
  (G40-100); locked avatars pre-accept (identity unlocks on accept).
- G40-35 moderation: contact-sharing allowed after accept, filtered before.

ENDPOINTS / BACKEND SEAMS
- Shared messaging transport + moderation seam; referral messages ("Add to MY
  Gophers") arrive here from Refer Yourself.
